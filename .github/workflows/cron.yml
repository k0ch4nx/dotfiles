# yaml-language-server: $schema=https://schemastore.org/github-workflow.json

name: Update flake.lock

on:
  schedule:
    - cron: "0 * * * *"

  workflow_dispatch:

concurrency:
  group: flake-update
  cancel-in-progress: false

jobs:
  check:
    name: Check for an existing update
    runs-on: ubuntu-latest
    permissions:
      pull-requests: read
    outputs:
      proceed: ${{ steps.pull-request.outputs.proceed }}

    steps:
      - name: Check for an open flake update pull request
        id: pull-request
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          open_pr_number="$(
            gh pr list \
              --repo "${GITHUB_REPOSITORY}" \
              --state open \
              --base main \
              --head automation/flake-update \
              --json number \
              --jq '.[0].number // empty'
          )"

          if [[ -n "${open_pr_number}" ]]; then
            printf 'An update pull request is already open: #%s\n' "${open_pr_number}"
            printf 'proceed=false\n' >>"${GITHUB_OUTPUT}"
          else
            printf 'proceed=true\n' >>"${GITHUB_OUTPUT}"
          fi

  update:
    name: Publish an update
    needs: check
    if: needs.check.outputs.proceed == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          ref: main

      - name: Install Nix
        uses: cachix/install-nix-action@v31
        with:
          github_access_token: ${{ secrets.GITHUB_TOKEN }}

      - name: Update flake.lock
        id: update
        env:
          DOTFILES_DIR: ${{ github.workspace }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          ./scripts/steps/070-update-flake-lock.sh

          unexpected_paths="$(git diff --name-only -- . ':(exclude)flake.lock')"
          if [[ -n "${unexpected_paths}" ]]; then
            printf 'Unexpected paths changed:\n%s\n' "${unexpected_paths}" >&2
            exit 1
          fi

          if git diff --quiet -- flake.lock; then
            printf 'changed=false\n' >>"${GITHUB_OUTPUT}"
          else
            printf 'changed=true\n' >>"${GITHUB_OUTPUT}"
          fi

      - name: Create the pull request
        id: create-pull-request
        if: steps.update.outputs.changed == 'true'
        uses: peter-evans/create-pull-request@v8
        with:
          token: ${{ secrets.FLAKE_UPDATE_TOKEN }}
          branch-token: ${{ secrets.GITHUB_TOKEN }}
          sign-commits: true
          branch: automation/flake-update
          base: main
          delete-branch: true
          add-paths: flake.lock
          commit-message: "chore: update flake.lock"
          title: "chore: update flake.lock"
          body: |
            Automated hourly flake input update.

            Build validation is handled by the pull request checks.

      - name: Enable auto-merge
        if: steps.create-pull-request.outputs.pull-request-operation == 'created'
        env:
          GH_TOKEN: ${{ secrets.FLAKE_UPDATE_TOKEN }}
          PR_NUMBER: ${{ steps.create-pull-request.outputs.pull-request-number }}
        run: gh pr merge --repo "${GITHUB_REPOSITORY}" --merge --auto "${PR_NUMBER}"
